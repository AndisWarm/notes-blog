# 08. 前端架构：Vue3 + Pinia + Axios + Element Plus

## 一、本章要解决的问题

前端怎么组织代码？页面怎么跳转？数据怎么流转？权限怎么控制？

## 二、技术栈选型

| 技术 | 作用 | 为什么选它 |
|---|---|---|
| Vue 3 | 前端框架 | 组合式 API、响应式、生态成熟 |
| Vite | 构建工具 | 启动快（秒级），HMR 热更新 |
| Vue Router | 路由 | 官方路由，支持路由守卫 |
| Pinia | 状态管理 | 比 Vuex 更简洁，TypeScript 友好 |
| Axios | HTTP 客户端 | 拦截器机制方便统一处理 token 和错误 |
| Element Plus | UI 组件库 | 中后台标配，组件全 |
| ECharts / vue-echarts | 图表 | 功能强大，支持地图/趋势/分布图 |

## 三、目录结构

```
web/src/
├── main.js              # 入口：创建 Vue 实例、挂载插件
├── App.vue              # 根组件：只有 <router-view>
├── router/              # 路由
│   ├── index.js         # 路由实例 + 全局守卫
│   └── modules/
│       └── security.js  # 安全域路由定义
├── api/                 # API 封装
│   ├── request.js       # Axios 实例 + 拦截器
│   ├── user.js          # 登录/用户接口
│   ├── securityTask.js  # 任务接口
│   ├── securityAlert.js # 预警接口
│   └── ...
├── pinia/               # 状态管理
│   ├── index.js         # Pinia 实例
│   └── modules/
│       ├── user.js      # 用户状态（token、userInfo）
│       └── security.js  # 安全域状态
├── view/                # 页面
│   ├── login/           # 登录页
│   ├── layout/          # 布局组件（侧边栏 + 顶栏）
│   ├── security/        # 安全域页面
│   │   ├── overview/    # 总览
│   │   ├── task/        # 任务列表 + 详情
│   │   ├── alert/       # 预警列表 + 详情
│   │   ├── history/     # 历史
│   │   ├── config/      # 安全配置
│   │   └── monitor/     # 实时监控
│   └── system/          # 系统域页面
│       ├── user/        # 用户管理
│       └── audit/       # 审计日志
├── components/          # 可复用组件
│   └── security/
│       ├── SecurityBreadcrumb.vue      # 面包屑
│       └── SecurityRelationGraph.vue   # 关系图
└── utils/               # 工具
    ├── auth-events.js   # 401 事件总线
    └── authState.js     # 登录态工具
```

## 四、入口流程

### 1. main.js

```javascript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import pinia from './pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'

const app = createApp(App)
app.use(router)
app.use(pinia)
app.use(ElementPlus)
app.mount('#app')
```

### 2. App.vue

```vue
<template>
  <router-view />  <!-- 所有页面都在这里渲染 -->
</template>
```

## 五、路由和权限

### 1. 路由定义

```javascript
// router/modules/security.js
export default [
    {
        path: '/security/overview',
        component: () => import('@/view/security/overview/index.vue'),
        meta: {
            title: '态势总览',
            requiresAuth: true,
            roles: ['ADMIN', 'MANAGER', 'USER'],  // 所有角色都能看
        },
    },
    {
        path: '/security/task',
        component: () => import('@/view/security/task/index.vue'),
        meta: {
            title: '检测任务',
            requiresAuth: true,
            roles: ['ADMIN', 'MANAGER', 'USER'],
        },
    },
    {
        path: '/security/config',
        component: () => import('@/view/security/config/index.vue'),
        meta: {
            title: '安全配置',
            requiresAuth: true,
            roles: ['ADMIN', 'MANAGER'],  // 只有 ADMIN 和 MANAGER
        },
    },
]
```

### 2. 全局路由守卫

```javascript
// router/index.js
import { useUserStore } from '@/pinia/modules/user'

router.beforeEach(async (to, from, next) => {
    const userStore = useUserStore()

    // 1. 如果页面不需要登录，直接放行
    if (!to.meta.requiresAuth) {
        next()
        return
    }

    // 2. 检查是否有 token
    if (!userStore.token) {
        next('/login')
        return
    }

    // 3. 如果有 token 但没有用户信息，先拉取
    if (!userStore.userInfo.username) {
        try {
            await userStore.getCurrentUser()
        } catch (err) {
            // token 失效，跳转登录页
            userStore.logout()
            next('/login')
            return
        }
    }

    // 4. 检查角色权限
    if (to.meta.roles && !to.meta.roles.includes(userStore.userInfo.roleCode)) {
        ElMessage.error('当前账号无权访问此页面')
        next('/security/overview')  // 跳到总览页
        return
    }

    next()
})
```

**执行时机**：每次路由跳转前都会执行。

## 六、Axios 封装和拦截器

文件：`web/src/api/request.js`

### 1. 创建 Axios 实例

```javascript
import axios from 'axios'

const request = axios.create({
    baseURL: '/api/v1',  // 所有请求都加 /api/v1 前缀
    timeout: 10000,
})
```

### 2. 请求拦截器

```javascript
request.interceptors.request.use(
    config => {
        // 自动带 token
        const token = localStorage.getItem('token')
        if (token) {
            config.headers.Authorization = `Bearer ${token}`
        }
        return config
    },
    error => Promise.reject(error)
)
```

### 3. 响应拦截器

```javascript
request.interceptors.response.use(
    response => {
        // 后端统一返回格式：{ code: 0, data: {...}, message: "success" }
        if (response.data.code !== 0) {
            ElMessage.error(response.data.message || '请求失败')
            return Promise.reject(new Error(response.data.message))
        }
        return response.data  // 只返回 data 部分
    },
    error => {
        if (error.response?.status === 401) {
            // 401：token 失效
            ElMessage.error('登录状态已失效，请重新登录')
            localStorage.removeItem('token')
            window.location.href = '/login'
        } else if (error.response?.status === 403) {
            // 403：无权限
            ElMessage.error('当前账号无权执行此操作')
        } else if (error.response?.status === 500) {
            // 500：服务器错误
            ElMessage.error('服务器错误，请稍后重试')
        }
        return Promise.reject(error)
    }
)

export default request
```

### 4. 业务 API 封装

```javascript
// api/securityTask.js
import request from './request'

export function createTask(data) {
    return request.post('/tasks', data)
}

export function getTaskList(params) {
    return request.get('/tasks', { params })
}

export function getTaskDetail(id) {
    return request.get(`/tasks/${id}`)
}

export function deleteTask(id) {
    return request.delete(`/tasks/${id}`)
}
```

**好处**：页面里不用写 URL 和参数处理，直接调用函数。

## 七、Pinia 状态管理

文件：`web/src/pinia/modules/user.js`

```javascript
import { defineStore } from 'pinia'
import { login, getCurrentUser, logout } from '@/api/user'

export const useUserStore = defineStore('user', {
    state: () => ({
        token: localStorage.getItem('token') || '',
        userInfo: {},
    }),

    actions: {
        async login(loginForm) {
            const res = await login(loginForm)
            this.token = res.token
            this.userInfo = res.userInfo
            localStorage.setItem('token', res.token)
        },

        async getCurrentUser() {
            const res = await getCurrentUser()
            this.userInfo = res.userInfo
        },

        async logout() {
            await logout()
            this.token = ''
            this.userInfo = {}
            localStorage.removeItem('token')
        },
    },
})
```

**在组件中使用**：

```vue
<script setup>
import { useUserStore } from '@/pinia/modules/user'

const userStore = useUserStore()

const handleLogin = async () => {
    await userStore.login({ username: 'admin', password: '123456' })
    router.push('/security/overview')
}
</script>
```

## 八、页面结构：以任务详情页为例

文件：`web/src/view/security/task/detail/index.vue`

### 1. 模板结构

```vue
<template>
  <div class="task-detail-page">
    <!-- 面包屑 -->
    <security-breadcrumb :items="['安全任务', '任务详情']" />

    <!-- 加载状态 -->
    <el-skeleton v-if="loading" :rows="10" animated />

    <!-- 错误状态 -->
    <el-result v-else-if="error" icon="error" :title="error" />

    <!-- 正常内容 -->
    <template v-else>
      <!-- 基本信息卡片 -->
      <el-card title="基本信息">
        <el-descriptions :column="2">
          <el-descriptions-item label="任务编号">{{ detail.taskNo }}</el-descriptions-item>
          <el-descriptions-item label="目标 IP">{{ detail.targetIp }}</el-descriptions-item>
          <el-descriptions-item label="任务状态">
            <el-tag :type="getStatusType(detail.taskStatus)">{{ detail.taskStatus }}</el-tag>
          </el-descriptions-item>
          <el-descriptions-item label="风险评分">
            <el-tag :type="getRiskType(detail.riskLevel)">{{ detail.scoreValue }}</el-tag>
          </el-descriptions-item>
        </el-descriptions>
      </el-card>

      <!-- 基础画像卡片 -->
      <el-card title="基础画像" v-if="detail.baseInfo">
        <el-descriptions :column="2">
          <el-descriptions-item label="国家">{{ detail.baseInfo.country }}</el-descriptions-item>
          <el-descriptions-item label="ISP">{{ detail.baseInfo.isp }}</el-descriptions-item>
        </el-descriptions>
      </el-card>

      <!-- 评分因子卡片 -->
      <el-card title="评分因子" v-if="detail.score">
        <el-table :data="scoreFactors">
          <el-table-column prop="label" label="维度" />
          <el-table-column prop="rawScore" label="原始分" />
          <el-table-column prop="weight" label="权重" />
          <el-table-column prop="contribution" label="贡献值" />
        </el-table>
      </el-card>

      <!-- 流量增强卡片 -->
      <el-card title="流量增强" v-if="detail.flow">
        <flow-detail-panel :flow="detail.flow" />
      </el-card>
    </template>
  </div>
</template>
```

### 2. 逻辑

```vue
<script setup>
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { getTaskDetail } from '@/api/securityTask'

const route = useRoute()
const taskId = route.params.id

const loading = ref(true)
const error = ref('')
const detail = ref({})

const fetchDetail = async () => {
    try {
        loading.value = true
        const res = await getTaskDetail(taskId)
        detail.value = res
    } catch (err) {
        error.value = err.message || '获取任务详情失败'
    } finally {
        loading.value = false
    }
}

onMounted(() => {
    fetchDetail()
})
</script>
```

**关键点**：
- `loading / error / detail` 三态管理
- `v-if="loading"` → 骨架屏
- `v-else-if="error"` → 错误提示
- `v-else` → 正常内容

## 九、ECharts 图表

### 1. 总览页风险分布饼图

```vue
<template>
  <v-chart :option="pieOption" style="height: 300px" />
</template>

<script setup>
import { computed } from 'vue'
import VChart from 'vue-echarts'
import 'echarts'

const props = defineProps(['data'])

const pieOption = computed(() => ({
    tooltip: { trigger: 'item' },
    series: [{
        type: 'pie',
        data: [
            { value: props.data.low, name: '低风险', itemStyle: { color: '#67C23A' } },
            { value: props.data.medium, name: '中风险', itemStyle: { color: '#E6A23C' } },
            { value: props.data.high, name: '高风险', itemStyle: { color: '#F56C6C' } },
            { value: props.data.critical, name: '严重风险', itemStyle: { color: '#8B0000' } },
        ],
    }],
}))
</script>
```

### 2. 实时监控页趋势图

```vue
<template>
  <v-chart :option="lineOption" style="height: 400px" />
</template>

<script setup>
const lineOption = computed(() => ({
    xAxis: {
        type: 'time',
    },
    yAxis: {
        type: 'value',
        name: '行为风险分',
        max: 100,
    },
    series: [{
        type: 'line',
        data: props.metrics.map(m => [m.timestamp, m.behaviorRisk]),
        smooth: true,
        areaStyle: {},
    }],
}))
</script>
```

## 十、本章 Q&A

**Q：为什么用 Pinia 不用 Vuex？**
A：
- Pinia 是 Vue 3 官方推荐的状态管理库
- 比 Vuex 更简洁（不用写 mutations）
- TypeScript 支持更好
- 支持 Composition API

**Q：为什么 token 存 localStorage 不存 cookie？**
A：
- localStorage 操作简单，`localStorage.setItem('token', xxx)`
- cookie 有 CSRF 风险，需要额外配置 SameSite
- localStorage 有 XSS 风险，但可以通过 CSP 和输入过滤缓解

**Q：前端怎么处理 401 和 403？**
A：
- 401：token 失效 → 清除 token，跳转登录页
- 403：无权限 → 提示"当前账号无权访问"，跳转总览页

**Q：为什么要在响应拦截器里统一处理错误？**
A：避免每个页面都写一遍错误处理逻辑。拦截器统一处理 401/403/500，页面里只用处理业务错误。

**Q：前端路由守卫和 Axios 拦截器都会处理 401，会不会重复？**
A：不会。
- 路由守卫处理**页面级** 401：跳转页面前检查 token
- Axios 拦截器处理**接口级** 401：请求返回 401 时清除 token

两者配合，保证用户体验和安全。

## 十一、下一步

读 [09-数据库设计.md](./09-数据库设计.md)，理解 13 张表的关系。